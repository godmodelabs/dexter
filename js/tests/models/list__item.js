define([
    'models/enemy',
    'jquery',
    'underscore',
    'backbone'
], function (
    Enemy,
    $, _,
    Backbone
) {

    describe('listItem', function() {
        //var list = new ListItem();

        it('should return default title', function() {
            return true;
        });
    });

});